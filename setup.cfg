# Helper file to handle all configs

[coverage:run]
# .coveragerc to control coverage.py and pytest-cov
omit =
    # Omit the tests
    */tests/*
    # Omit generated versioneer
    tailoredcc/_version.py

[flake8]
# Flake8, PyFlakes, etc
max-line-length = 100

[aliases]
test = pytest